services:
  - type: web
    name: legalix-mandat-bot
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeploy: true
    healthCheckPath: /health
    disk:
      name: legalix-mandat-data
      mountPath: /var/data
      sizeGB: 1
